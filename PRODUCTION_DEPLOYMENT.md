# Production Deployment Guide

This guide is superseded by [VERCEL_DEPLOYMENT.md](./VERCEL_DEPLOYMENT.md).

The repository now deploys both `frontend/` and `backend/` on Vercel, with the backend exposed under `/api`.
